% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_bp.R
\name{.valid_bpDemoInput}
\alias{.valid_bpDemoInput}
\title{Validate Demographic Inputs for BP Functions (Internal)}
\usage{
.valid_bpDemoInput(
  bp_type = c("systolic", "diastolic"),
  sex,
  male_code,
  female_code,
  height_z,
  height_limit = 5,
  age_years,
  ref = c("Fourth Report", "NICE/BHF"),
  .quiet = FALSE
)
}
\arguments{
\item{bp_type}{Character; \code{"systolic"} or \code{"diastolic"}.}

\item{sex}{Vector of sex codes in your data. Will be mapped to \code{"male"} or \code{"female"}
using \code{male_code} and \code{female_code}. Values not matching these codes become \code{NA}.}

\item{male_code, female_code}{Scalar values that identify the male and female codes in \code{sex}
(e.g., \code{1} and \code{2}, or \code{"M"} and \code{"F"}).}

\item{height_z}{Numeric vector of height z-scores. Must already be z-transformed (e.g., US CDC, UK-WHO).}

\item{height_limit}{Positive numeric; z-scores with \code{|z| > height_limit} are treated as invalid
and set to \code{NA}. Default \code{5}.}

\item{age_years}{Numeric vector of ages in years.}

\item{ref}{Reference: \code{"Fourth Report"} (children/adolescents, 0-17) or \code{"NICE/BHF"} (adults, >17).}

\item{.quiet}{Logical; suppress validation messages (default \code{FALSE}).}
}
\value{
A list with normalized elements: \code{bp_type}, \code{sex} (\code{"male"} or \code{"female"} or \code{NA}),
  \code{age_years} (invalid ages set to \code{NA} given \code{ref}), \code{height_z} (out-of-bound z set to \code{NA}), and \code{ref}.
}
\description{
Internal helper function to validate and standardize demographic inputs
for blood pressure calculations. Checks age ranges, sex coding, and height
z-scores against specified limits.
}
\seealso{
Other BP functions:
\code{\link{family_bp}},
\code{\link[=get_bp]{get_bp()}},
\code{\link[=get_bpCategory]{get_bpCategory()}},
\code{\link[=get_bpExpected]{get_bpExpected()}},
\code{\link[=get_bpRelative]{get_bpRelative()}}
}
\concept{BP functions}
\keyword{internal}
