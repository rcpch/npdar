% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_corrMat.R
\name{get_corrMat}
\alias{get_corrMat}
\title{Correlation Matrix with Plotly}
\usage{
get_corrMat(
  data,
  method = c("pearson", "spearman"),
  use = c("pairwise.complete.obs", "complete.obs", "everything", "all.obs",
    "na.or.complete"),
  digits = 2,
  show_stars = TRUE,
  show_values = TRUE,
  colors = c("#11A7F2", "#FFFFFF", "#E00087"),
  ...
)
}
\arguments{
\item{data}{A data frame or matrix containing only numeric variables.}

\item{method}{Correlation method: \code{"pearson"} or \code{"spearman"},
passed directly to \code{stats::cor()} and \code{stats::cor.test()}.}

\item{use}{Missing-data handling passed directly to \code{stats::cor()}.
One of \code{"pairwise.complete.obs"}, \code{"complete.obs"},
\code{"everything"}, \code{"all.obs"}, or \code{"na.or.complete"}.}

\item{digits}{Number of decimal places for correlation labels.}

\item{show_stars}{Logical; display significance stars (*, **, ***) based on p-values.}

\item{show_values}{Logical; display correlation values inside tiles.}

\item{colors}{A vector of colours for the heatmap colour scale.
Defaults to NPDA colours.}

\item{...}{Additional arguments passed to \code{plotly::plot_ly()}.
These can override default plotly arguments
such as \code{showscale}, or \code{colorbar}.}
}
\value{
A \pkg{plotly} heatmap object.
}
\description{
Computes a correlation matrix and corresponding p-values, and visualises the
lower triangle as an interactive heatmap using \pkg{plotly}. Correlation values
and significance stars can be displayed inside each tile.
}
\examples{
# Basic correlation matrix
get_corrMat(mtcars)

# Spearman correlation without tile labels, with additional plotly arguments
get_corrMat(mtcars,
            method = "spearman",
            show_values = FALSE,
            show_stars = TRUE,
            colorbar = list(title = "Correlation")) |>
plotly::layout(annotations = list(
  text = paste0("<i><b>Note:</b>",
                "*: p<0.05, **: p<0.01, ***: p<0.001.</i>"),
  x = 1, y = 1, xref = "paper", yref = "paper",
  xanchor = "right", yanchor = "top",
  showarrow = FALSE))

}
